---
const { article } = Astro.props
---
 
<a  href={`/blog/${article.slug}`} class="h-80 rounded-md cursor-pointer group flex flex-col bg-zinc-800  hover:-translate-y-px motion-reduce:hover:transform-none transition ease-in-out duration-200">
  <img
    class="object-cover object-center rounded-t-md w-full h-48"
    src={`${article.image.filename}/m/360x240`}
    alt={article.image.alt}
  />
  <div class="p-6  text-white w-full h-full flex flex-col justify-center">
    <h1 class="text-2xl font-semibold lg:text-3xl">
      {article.title}
    </h1>
    <div class="flex gap-4 text-zinc-300 items-center">
    <p class="line-clamp-2 mt-2">
      {article.description}
    </p>
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-current mt-2 group-hover:rotate-45 transition ease-in-out duration-200">
      <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 19.5 15-15m0 0H8.25m11.25 0v11.25" />
    </svg>
  </div>
    
  </div>
</a>